<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{% block title %}SLH labo #2{% endblock %}</title>
    <link href="css/style.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <style>{% block style %}{% endblock %}</style>
  </head>
  <body>
    <nav class="navbar navbar-light bg-light">
      <div class="container-fluid">
        <a class="navbar-brand" href="{% if user %}/home{% else %}/{% endif %}">SLH - Labo #2</a>
        <div>
          {% if user %}
          <span>{{ user.name }}</span>
        <a href="/reset" class="btn btn-danger me-2">reset DB (admin only)</a>
          <a href="/logout" class="btn btn-outline-primary me-2">Logout</a>
          {% else %}
          <a href="/login" class="btn btn-outline-primary me-2">Login</a>
          <a href="/register" class="btn btn-outline-secondary">Register</a>
          {% endif %}
        </div>
      </div>
    </nav>

    <div class="container mt-5">
      {% if success_message %}
      <div class="alert alert-success text-center alert-dismissible">
        {{success_message}}
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
      </div>
      {% endif %}

      {% if error_message %}
      <div class="alert alert-danger text-center alert-dismissible">
        {{error_message}}
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
      </div>
      {% endif %}

      {% block body %}
      <h1>{{ title }}</h1>
      {% endblock %}
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
